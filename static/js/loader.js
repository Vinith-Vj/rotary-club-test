document.addEventListener("DOMContentLoaded", function () {
    // Simulating the loader hide delay
    setTimeout(() => {
      const loader = document.getElementById('ftco-loader');
      loader.classList.add('hide');
    }, 1000); // Adjust delay time as needed
  });
  